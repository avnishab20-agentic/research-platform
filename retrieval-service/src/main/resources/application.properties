spring.application.name=retrieval-service
server.port=8081

# Revalidate static files on every load (Last-Modified makes it a cheap 304), so a
# deploy shows up without a hard refresh -- browsers were running a stale app.js.
spring.web.resources.cache.cachecontrol.no-cache=true
