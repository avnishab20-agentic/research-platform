apiVersion: v1
kind: Service
metadata:
  name: extractor
  namespace: namespace-workflow-1790106905740
  labels:
    app.kubernetes.io/name: extractor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: extractor
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
