package com.comeback.researchplatform.agentservice.eval;

/** PLAN's five fabrication-injection corruption categories. OVERREACH is
 *  named in PLAN as "the hardest and most realistic" -- a claim that's
 *  still literally defensible ("analysts" -> "analysts unanimously") but
 *  overstates its own evidence, which is exactly the kind of subtle
 *  fabrication a real Critic has to catch, not just an obviously wrong
 *  number. */
public enum CorruptionType { SWAP_NUMBER, INVERT, SWAP_ENTITY, FABRICATE, OVERREACH }
