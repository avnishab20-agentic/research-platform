apiVersion: v1
kind: Service
metadata:
  name: control-plane
  namespace: namespace-workflow-1790106905740
  labels:
    app.kubernetes.io/name: control-plane
spec:
  # Reached from the browser only through the front door (k8s/front-door.yaml).
  type: ClusterIP
  selector:
    app.kubernetes.io/name: control-plane
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
