# Taste
- Documentation quality bar: claims must be factually accurate against the actual source code, and the writing must be easy to understand for a non-technical "school person" reader; user explicitly asks that docs be checked for correctness and refined for clarity. Confidence: 0.8
- In Markdown docs with clickable internal links, the links must actually resolve when clicked — the user tests links in their own environment (editor/browser, not just GitHub), so anchors must work across renderers, not just under GitHub's slug rules. Confidence: 0.6
- When the user asks to "check if X is good, if not refine it," they expect the agent to run the full loop end-to-end — verify against ground truth, then fix whatever is wrong or unclear — without stopping to ask for approval. Confidence: 0.7
- When remaining work splits into independent chunks, explicitly wants it parallelized by spawning multiple subagents (e.g., "spawn multiple agents to do it") rather than doing everything sequentially. Confidence: 0.8
- Writes short, casual, directive instructions (e.g., "yo check if …", "continue") and delegates judgment calls in a single sentence. Confidence: 0.5
- User runs a "graphify" tool that generates a code-graph output in `graphify-out/` (manifest.json, GRAPH_REPORT.md, graph.json) in the research-platform repo, and expects the agent to consult it as a codebase map before answering codebase questions — proactively state whether graphify output was used. Confidence: 0.5
- For documentation tasks, only touch docs — never modify source code. Confidence: 0.6
- Cross-session continuation workflow: a "continue" instruction means resume in-progress work from the repo's session log / docs/PLAN.md without re-briefing; each working step ends by updating the session log and committing. Confidence: 0.7
- Verify-then-commit loop for feature work: compile and run module-scoped tests first (`mvn -q -pl <module> -am test`), then bring up the docker compose stack, poll health endpoints until ready (instead of fixed sleeps), verify the endpoint end-to-end with curl (including cache-hit behavior), and only then update the log and commit. Confidence: 0.7
- Environment handling: when the Docker daemon isn't running on macOS, start Docker Desktop (`open -a Docker`) and poll `docker info` in a loop until ready before any compose operation. Confidence: 0.6
- Debugging style: when live behavior contradicts expectations, isolate variables systematically before concluding (direct curl vs. service fetch, proxy env, custom User-Agent/WAF blocking, app logs); flag silently swallowed exceptions and unvalidated heuristics as issues rather than accepting them. Confidence: 0.6
